steps:
  # 1. Autenticarse en Artifact Registry
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

  # 2. Construir la imagen con Docker (desde carpeta java-hello-world)
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-f',
        './java-hello-world/Dockerfile',
        '-t',
        'us-central1-docker.pkg.dev/abriendopuertas/java-backend-repo/java-backend:$COMMIT_SHA',
        './java-hello-world'
      ]

  # 3. Subir la imagen a Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'push',
        'us-central1-docker.pkg.dev/abriendopuertas/java-backend-repo/java-backend:$COMMIT_SHA'
      ]

  # 4. Desplegar en Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run',
        'deploy',
        'java-backend',
        '--image=us-central1-docker.pkg.dev/abriendopuertas/java-backend-repo/java-backend:$COMMIT_SHA',
        '--platform=managed',
        '--region=us-central1',
        '--allow-unauthenticated',
        '--quiet'
      ]

images:
  - 'us-central1-docker.pkg.dev/abriendopuertas/java-backend-repo/java-backend:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
